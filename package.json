{
  "name": "pariksha",
  "version": "1.0.0",
  "private": true,
  "description": "UPSC CSE practice platform — static frontend, no backend.",
  "scripts": {
    "build": "node build.js",
    "manifest": "node scripts/generate_content_manifest.js config/content_manifest.json",
    "serve": "python3 scripts/dev_server.py 8001"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

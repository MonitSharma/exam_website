{
  "name": "pariksha",
  "version": "1.0.0",
  "private": true,
  "description": "UPSC CSE practice platform — static frontend, no backend.",
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8001"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
